import { Typography } from "@mui/material";
import { TableCode } from "../components/render";
const A = {};
export default function PageOne() {
    console.log(A);
    return <TableCode>{A}</TableCode>;
}
